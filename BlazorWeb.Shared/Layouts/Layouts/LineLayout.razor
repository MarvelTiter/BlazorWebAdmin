@using BlazorWeb.Shared.Layouts.LayoutComponents
@inherits LayoutBase
@*<section style="display:flex;width:100%;">
    <aside>
        <SideBar @ref=SideBar Class="classic">
            <TopBanner>
                <Banner OnToggleCollapse="e=>HandleToggleCollapse(e)"></Banner>
            </TopBanner>
        </SideBar>
    </aside>
</section>*@
<section style="display:flex;flex-direction:column; width:100vw;height:100vh;">
    <header style="display:flex;width:100%;height: 50px;">
        <Banner ShowCollapseButton=false Class="line"></Banner>
        <ScrollBar>
            <SideBar Class="line" Horizontal></SideBar>
        </ScrollBar>
        <NavMenu Class="line"></NavMenu>
    </header>
    <main style="padding:16px;height: calc(100vh - 50px);">
        <AppMain ChildContent="@(ChildContent!)">
        </AppMain>
    </main>
</section>
