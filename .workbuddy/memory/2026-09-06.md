# 2026-09-06

## 反射→源生成器 改造点全量盘点（仅分析，未改代码）

扫描范围：`src/Shared/BlazorTemplates`（排除 obj 生成物）。输出优先级大纲给用户。

### 结论分层
- **P0 AOT 硬阻塞（Expression.Compile / MakeGenericMethod，生成器救不了，必须改 API 形状）**
  1. `Components/InputBuilder.cs` — `typeof(IUIService).GetMethod(...)` + `MakeGenericMethod` + `Expression.Lambda(...).Compile()`，按 ColumnInfo 缓存。建议改 TypeCode switch 直接泛型分发。
  2. `UI/Builders/BindableComponentBuilder.cs` / `SelectComponentBuilder.cs` — `expression.Compile()` 造 getter/setter。建议新增 `.Bind(getter, setter)` 双委托重载。
  3. `BlazorTemplate.UI.AntBlazor/UIService.cs:187,189,540,556` — `labelLambda.Compile()` / `valueLambda.Compile()`；调用点本身就是 lambda，API 改成收 `Func<TItem,string>` 即可零反射（低成本）。
  4. `UI/Table/ColumnInfo.cs` `MakeExpression<TData,TProp>` — 按字符串名 `Expression.Property`。
  5. `Utils/ConditionUnitHelper.cs` — `typeof(T).GetProperty(name)` + `Enum.Parse` + `Convert.ChangeType`；属性集编译期已知，可生成 `switch(name)` 强类型构造。
- **P1 高收益特性扫描（生成器直接产出注册表）**
  1. `Routers/PagesService.cs` — `AppConst.AllAssemblies` → `ExportedTypes` → Route/PageInfo/PageGroup/Authorize/AllowAnonymous/Layout 六类特性扫描。**收益最大**。
  2. `ProjectInit.ScanRazorLibraryAssembly()` — `GetEntryAssembly().GetReferencedAssemblies().Select(Assembly.Load)`。
  3. `Locales/EmbeddedJson/EmbeddedJsonLocalizerFactory.cs` — `Assembly.Load` + `GetManifestResourceNames()` + `FindLangName` 的 `GetCustomAttribute<LangNameAttribute>`。
  4. `Aop/AopPermissionCheck.cs:60` / `Aop/AopLogger.cs:20` — 运行时 `GetCustomAttribute`（代理类已由 AutoAopProxy 生成，可顺带生成特性常量）。
  5. `Routers/RouterStore.cs:189` — `obj.GetType().GetCustomAttribute<TagTitleAttribute>()`。
- **P2 收尾 / 可选**
  - `TableColumnContext.GenerateColumnsReflection` 仍在（TableColumnGenerator 已通过 `RegisterGeneratedColumns` + ModuleInitializer 覆盖），需加覆盖率诊断。
  - `Utils/EnumHelper.cs` 的 `GetDisplayName/ParseDictionary` 反射，可并入 `EnumDisplayProviderGenerator`。
  - `Common/ExpressionHelper.cs` `ExtractProperty` 的 `type.GetProperty` —— 调用点都是 `x => x.Prop`，改 API 即可，不必生成器。
- **误报 / 无需处理**
  - `JsonLocalizer.cs`、`JsonInfo.cs` 命中均为 `JsonElement.TryGetProperty/Get*`，非反射。
  - `ColumnInfo.IsEnum/Nullable/UnderlyingType`、`InputBuilderHelper.GetInputType` 的 `Type.GetTypeCode` switch、`ObjectExtensions.ConvertTo`、`StaticCache/SessionCache` 均为 AOT 安全。

### 复用套路（源自 ModelPageCapabilityGenerator）
基类保留反射 fallback + `virtual` 钩子 → 生成器发 `partial override` → 诊断码分段 → 生成器工程不得引用宿主类型（字符串常量 + `global::` 前缀，`FullyQualifiedFormat` 已自带前缀，勿重复加）→ 验证必须带 `--no-incremental`。

### 备注
项目内已有生成器：SvgContentClassGenerator、EnumDisplayProviderGenerator、TableColumnGenerator、ModelPageCapabilityGenerator；外部还有 AutoAopProxyGenerator、AutoPageStateContainerGenerator。
