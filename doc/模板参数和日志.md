## UseClientProject

是否引用.Client项目
1. 主项目Program.cs
1. 主项目Routers.cs
1. 主项目的csproj

## ExcludeDefaultPages
默认的页面
1. /user/index
1. /operation/index
1. /rolepermission/index
1. /permission/index

去除默认的页面，则不会在Programe.cs中注册这些页面

## 模板日志记录

### v2026.08.28
- Svg生成器去重处理
- csproj配置为如下
```xml
<ItemGroup>
	<AdditionalFiles Include="wwwroot/**/*.svg" />
</ItemGroup>
```

### v2026.08.26
- 优化AntTable的行编辑和单元格编辑功能

### v2026.08.14
- 重构模板
- 统一使用`ILookupService`
- 添加生成器处理Enum，生成相应的LookupServiceProvider

### v2026.08.10
- `JsComponentBase`添加`RewriteModuleName`虚方法
- `FlyoutOptions`增加对打开的组件的属性设置

### v2026.08.04
- 使用生成器处理svg文件。如果项目中有svg文件需要处理，在csproj中添加如下配置
```xml
<ItemGroup>
	<AdditionalFiles Include="**/*.svg" />
</ItemGroup>
```

### v2026.08.03
- 升级.NET10
