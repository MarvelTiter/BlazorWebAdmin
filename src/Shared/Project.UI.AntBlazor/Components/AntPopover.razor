<Popover Title="@Options.Title" Trigger="@(new AntDesign.Trigger[] { AntDesign.Trigger.Click})" ContentTemplate="@Options.Content">
    @Options.Trigger
</Popover>
@code {
    [Parameter, NotNull] public PopoverOptions? Options { get; set; }
}
