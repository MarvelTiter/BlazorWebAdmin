@typeparam TPower where TPower : class, IPower, new()
@typeparam TRole where TRole : class, IRole, new()
@typeparam TPermissionService where TPermissionService : IPermissionService<TPower, TRole>
@inherits ModelPage<TPower, GenericRequest<TPower>>
<DefaultTableHeader Options="@(Options)" DownloadImportTemplate="@DownloadImportTemplate">
    @UI.BuildButton(this).Primary().Text(Localizer["PermissionSetting.AddRoot"]).OnClick(AddRootPowerAsync).Render()
</DefaultTableHeader>
@TableFragment
