@page "/testpageaction"
@using Project.Constraints.Common.Attributes
@using System.Diagnostics.CodeAnalysis
@using Project.Constraints.Page
@using Project.Constraints.PageHelper
@using Project.Constraints.UI
@inherits AppComponentBase
@attribute [PageInfo(Title = "PageAction测试", Icon = "fa fa-question-circle-o", GroupId = "test")]
<div style="display:flex;flex-direction:column;gap:5px;">
    @foreach (var item in contents)
    {
        <span @key="@item.Item1">@($"{item.Item1:yyyy-MM-dd HH:mm:ss}") => @item.Item2</span>
    }
</div>
@UI.BuildDatePicker(this).Bind(() => date).Render()
@date
@code {
    List<(DateTime, string)> contents = [];
    DateTime? date;

}
