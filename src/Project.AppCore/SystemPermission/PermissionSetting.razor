@typeparam TPower where TPower : class, IPower, new()
    @typeparam TRole where TRole : class, IRole, new()
@inherits ModelPage<TPower, GenericRequest<TPower>>
@TableFragment

@code {

    RenderFragment<FormItemContext> IconSelect()
    {
        return ctx =>
        {
            return @<Project.AppCore.SystemPermission.Component.PowerIconSelector Context="ctx"></Project.AppCore.SystemPermission.Component.PowerIconSelector>;
        };
    }

}